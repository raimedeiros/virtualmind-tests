import React from 'react';
import ReactDOM from 'react-dom';
import BigForm from './App';

ReactDOM.render(
  <React.StrictMode>
    <BigForm />
  </React.StrictMode>,
  document.getElementById('root')
);